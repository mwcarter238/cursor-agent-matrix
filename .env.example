# Copy to .env and adjust. Used by docker-compose.
POSTGRES_PASSWORD=pharma

# JWT signing key — MUST be a long random secret in production (>= 32 chars).
JWT_KEY=change-me-to-a-long-random-secret-at-least-32-chars
