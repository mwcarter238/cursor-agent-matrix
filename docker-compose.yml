services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8787:8787"
    env_file:
      - .env
    environment:
      API_HOST: 0.0.0.0
      API_PORT: "8787"
